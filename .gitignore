# Données volumineuses
data/fraudTest.csv
data/fraudTest.ods

# Fichiers générés (préparation + modèles)
work/
model/
other_projects/

# Environnements virtuels
.venv/
.venv-api/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Environnement (.env.template est commité, sert de doc pour .env.test/.env.production)
.env
.env.test
.env.production
api/.env

# MCP (contient des credentials en clair — cf. .mcp.json.template)
.mcp.json

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# Airflow
airflow/logs/

# Claude Code (config/état local de session, pas du contenu projet)
.claude/

# Tests
.pytest_cache/

# Vidéo de démo (828 Mo — dépasse la limite GitHub de 100 Mo/fichier sans Git LFS)
docs/enonce/*.mov

# Notes de travail personnelles (pas un livrable du projet)
docs/travail/
