# Données volumineuses
data/fraudTest.csv
data/fraudTest.ods
*.mp4

# Fichiers générés (préparation + modèles)
work/
model/
other_projects/

# Environnements virtuels
.venv/
.venv-api/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Environnement — un fichier .env.test/.env.production par application (api/, airflow/,
# dashboard/), chaque *.env.template correspondant reste commité
.env
api/.env
api/.env.test
api/.env.production
airflow/.env.test
airflow/.env.production
dashboard/.env.test
dashboard/.env.production

# MCP (contient des credentials en clair — cf. .mcp.json.template)
.mcp.json

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# Airflow
airflow/logs/

# Claude Code (config/état local de session, pas du contenu projet)
.claude/

# Tests
.pytest_cache/

# Notes de travail personnelles (pas un livrable du projet)
docs/travail/
